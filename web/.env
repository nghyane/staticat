# PUBLIC build-time config (inlined into the client bundle via $env/static/public).
# These are public URLs, not secrets — safe to commit. R2 write creds live in the
# worker, never here. Override per-build with shell env vars if needed.
PUBLIC_DATA_BASE=https://pub-9b90fa87aa1a44fa8c4ccccf49ace9b0.r2.dev
PUBLIC_SITE_URL=https://watchdex.pages.dev
