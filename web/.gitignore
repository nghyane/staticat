node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env.*
!.env.example
!.env.test
# .env here holds only PUBLIC build constants (public URLs, no secrets) — commit it
!.env

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# generated by ingest (R2 stand-in for dev; prod serves from R2)
/static/v1
